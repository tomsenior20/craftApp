version: '3.8'

services:
  db:
    container_name: sqllitedb
    image: keinos/sqlite3
    restart: always
    environment:
      DB_Path: ${SQLLite_DB_PATH}
    ports:
      - ${PORT}

  app:
    container_name: app
    depends_on:
      - db
    restart: always
    build:
      dockerfile: Dockerfile
    ports:
      - ${PORT}
    command: sh -c "npx node server/server.js && npm run build"
